//
//  CanvasView.swift
//  Appdev
//
//  Created by Prabhnoor Kaur on 25/08/25.
//

/*import SwiftUI

struct CanvasView: View {
    var body: some View {
        Canvas{context,size in context.draw(Text("yolo"),at:CGPoint(x: size.width/2, y: size.height/2))
            context.stroke(Path(RoundedRectangle(cornerSize: .init(width: 10, height: 10))), with: .color(Color.red), style: StrokeStyle()
        }.frame(width:400,height:400)
            .border(Color.red)
    }
}

#Preview {
    CanvasView()
}
*/
